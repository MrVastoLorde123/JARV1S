# M7 Branch Cleanup

The implementation checkpoint `feature/m7-10-execution-semantics` is the clean M7.10 code boundary.

The documentation-complete M7 baseline is `milestone/m7-complete`.

Canonical M7 milestone branches remain historical checkpoints. Suffixed experimental branches are disposable once unique work is confirmed absent.

M7 semantics, provenance identities, and the `READY` versus `EXECUTED` boundary must not be changed during cleanup.
